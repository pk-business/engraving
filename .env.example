### Example environment variables for the frontend (safe to commit)
# Copy this file to `.env.local` and fill in any secrets locally (do NOT commit `.env.local`).

# Base URL for your Strapi instance
## Local dev override (optional)
# Copy to .env.local and edit when you need to point to a different Strapi instance
VITE_STRAPI_URL=http://localhost:1337

# OR point to your deployed Render service (default used by this project):
# VITE_STRAPI_URL=https://pk-engrave-service.onrender.com

# Enable proxy usage (frontend will call /proxy/* instead of Strapi directly). Useful when running the local proxy server.
# VITE_USE_PROXY=false

# Optional: API token for seeding or server-side calls. Keep this secret and store it in `.env.local`.
# STRAPI_API_TOKEN=your_strapi_api_token_here

# Notes:
# - Vite exposes variables prefixed with VITE_ to the client-side code.
# - Never commit real API tokens or secrets to the repository.
