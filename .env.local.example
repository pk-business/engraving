### Local env example (DO NOT commit `.env.local`)
# Copy this file to `.env.local` and update values for your machine.

VITE_STRAPI_URL=http://localhost:1337
STRAPI_API_TOKEN=

# After creating `.env.local` restart the dev server: `npm run dev`.
